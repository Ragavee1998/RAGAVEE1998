#include<stdio.h>
int main()
{
char input;
printf("enter the input:");
scanf("%s",&input);
if(((input>='a')&&(input<='z'))||((input>='A')&&(input<='Z'))
printf("the input is alphabet");
else
printf("the input is alphabet");
return 0;
}
